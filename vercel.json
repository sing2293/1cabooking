{
  "buildCommand": "cd 1cabooking && npm install && npm run build",
  "outputDirectory": "1cabooking/dist",
  "installCommand": "echo skip",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
